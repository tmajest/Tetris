/**
 * Common settings for tetris.
 */
(function(settings) {
    settings.screenWidth = 301;
    settings.screenHeight = 721;
    settings.tileSize = 30;
    settings.fallRate = 40;
})(window.settings = window.settings || {});
